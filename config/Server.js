/**
 * Configure your API here. Add your API url and accessToken here then use this object anywhere.
 */

const Api = {
  url: "https://yourServer.com/api/",
  header: {
    "token": "YourApiToken",
    "Content-Type": "application/json" // Content type of the request
  }
}

export default Api
